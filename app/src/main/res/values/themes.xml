<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ElibraryPetik" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Main Brand Colors -->
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorPrimaryVariant">@color/primary_blue_light</item>
        <item name="colorOnPrimary">@color/white</item>
        
        <!-- Secondary brand color -->
        <item name="colorSecondary">@color/accent_blue</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="colorAccent">@color/accent_blue</item>
        
        <!-- Warna Indikator Aktif -->
        <item name="colorSecondaryContainer">#DBEAFE</item>
        <item name="colorOnSecondaryContainer">@color/primary_blue</item>
        
        <!-- Warna untuk komponen kontrol -->
        <item name="android:colorControlActivated">@color/accent_blue</item>
        <item name="android:colorControlHighlight">#201E3C72</item>

        <!-- Background and Surface -->
        <item name="android:windowBackground">@color/background_light</item>
        <item name="colorSurface">@color/surface_white</item>

        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/primary_blue</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>

        <!-- Navigation Bar -->
        <item name="fontFamily">@font/poppins_regular</item>
        <item name="drawerArrowStyle">@style/DrawerIconStyle</item>
        <item name="chipStyle">@style/CustomChipStyle</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.ElibraryPetik" parent="Base.Theme.ElibraryPetik" />

    <!-- Drawer Icon Style -->
    <style name="DrawerIconStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@color/white</item>
    </style>

    <!-- Custom Chip Style -->
    <style name="CustomChipStyle" parent="Widget.Material3.Chip.Filter">
        <item name="checkedIconVisible">false</item>
        <item name="android:fontFamily">@font/poppins_medium</item>
        <item name="android:textSize">12sp</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/accent_blue</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.ElibraryPetik</item>
    </style>
</resources>
